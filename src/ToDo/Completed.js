import React from 'react'

const Completed = () => {
  return (
    <div>
      {
        // some kind of if else statement goes here to separate whats completed and what is not.
        // don't forget to add the time it was submitted.
      }
    </div>
  )
}

export default Completed
